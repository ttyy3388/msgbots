package org.beuwi.simulator.platform.application.views.actions;

public class ChangeEditorTabAction
{
    // To Right
	public static void update()
	{
		/* IEditorPane pane = EditorAreaPart.getSelectedPane();

		List<Tab> list = pane.getTabs();

		int index = pane.getSelectedIndex();

        if ((list.size() - 1) + " : " + (index + 1));

		// 다음 탭이 있다면
		/* if (list.size() get(index + 1) != null)
        {
            pane.selectTab(list.get(index + 1));
        }
		else
        {
            pane.selectTab(list.get(0));
        } */
	}
}
