package org.beuwi.msgbots.platform.gui.base;

import javafx.beans.NamedArg;
import javafx.scene.control.TextArea;
import javafx.scene.control.TextField;

public class Anchor {
    /* public static final Anchor EMPTY = new Anchor(null, null, null, null);

    private Double top;
    private Double right;
    private Double bottom;
    private Double left;

    public Anchor(@NamedArg("top") Double top,
                  @NamedArg("right") Double right,
                  @NamedArg("bottom") Double bottom,
                  @NamedArg("left") Double left) {
        this.top = top;
        this.right = right;
        this.bottom = bottom;
        this.left = left;
    }

    public Double getTop() {
        return top;
    }
    public Double getRight() {
        return right;
    }
    public Double getBottom() {
        return bottom;
    }
    public Double getLeft() {
        return left;
    } */

    /* @Override
    public boolean equals(Object object) {
        if (object instanceof Anchor) {
            Anchor target = (Anchor) object;
        }
    } */
}
